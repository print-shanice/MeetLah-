# MeetLah! Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Login/     │  │   Calendar   │  │   Meetup     │         │
│  │   Signup     │  │     View     │  │   Creation   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  Attendance  │  │    Streak    │  │  Punishment  │         │
│  │   Tracking   │  │   Display    │  │  Assignment  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      NEXT.JS APP ROUTER                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📁 app/                                                        │
│    ├── auth/callback/          ← OAuth callback                │
│    ├── calendar/[id]/          ← Calendar pages                │
│    ├── join/[code]/            ← Join calendar                 │
│    ├── login/                  ← Login page                    │
│    └── signup/                 ← Signup page                   │
│                                                                 │
│  🧩 components/                                                │
│    ├── calendar/                                               │
│    │   ├── attendance-modal.tsx    🆕 Attendance tracking      │
│    │   ├── streak-display.tsx      🆕 Streak visualization     │
│    │   ├── calendar-grid.tsx       Calendar view               │
│    │   ├── event-modal.tsx         Event creation              │
│    │   └── meetup-modal.tsx        Meetup creation             │
│    └── ui/                      shadcn/ui components           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     SERVER ACTIONS (lib/actions/)               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📝 calendar.ts                                                │
│    ├── getUserCalendars()                                      │
│    ├── createCalendar()                                        │
│    ├── joinCalendarByCode()                                    │
│    ├── getCalendarDetails()        🔄 Enhanced with streaks    │
│    ├── createEvent()                                           │
│    ├── createMeetup()              🔄 Updates streak           │
│    ├── deleteEvent()                                           │
│    ├── markAttendance()            🆕 Mark late/on-time        │
│    ├── assignPunishment()          🆕 Assign punishment        │
│    └── completePunishment()        🆕 Mark as completed        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     SUPABASE (Backend)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🔐 AUTHENTICATION                                             │
│    ├── Google OAuth                                            │
│    ├── Facebook OAuth                                          │
│    └── Session Management                                      │
│                                                                 │
│  🗄️  DATABASE (PostgreSQL)                                    │
│                                                                 │
│    Core Tables:                                                │
│    ├── profiles                  User accounts                 │
│    ├── calendars                 Shared calendars              │
│    ├── calendar_members          Membership                    │
│    └── events                    Personal & meetup events      │
│                                                                 │
│    Feature Tables (v2):                                        │
│    ├── calendar_streaks      🆕  Monthly streak tracking       │
│    ├── meetup_participants   🔄  + attendance fields           │
│    └── event_punishments     🆕  Assigned punishments          │
│                                                                 │
│  ⚙️  FUNCTIONS                                                │
│    ├── handle_new_user()         Auto-create profile           │
│    ├── update_updated_at()       Timestamp trigger             │
│    └── update_calendar_streak()  🆕 Streak calculation         │
│                                                                 │
│  🔒 ROW LEVEL SECURITY (RLS)                                   │
│    └── All tables protected                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Examples

### 1. Creating a Meetup & Updating Streak

```
User clicks "Create Meetup"
         │
         ▼
MeetupModal (component)
         │
         ▼
createMeetup() action
         │
         ├─→ Insert into events table (type: meetup)
         │
         ├─→ Insert participants into meetup_participants
         │
         └─→ Call update_calendar_streak() function
                   │
                   ├─→ Check last meetup month
                   │
                   ├─→ Calculate if consecutive
                   │
                   └─→ Update calendar_streaks table
                             │
                             ▼
                   Streak displayed in sidebar ✨
```

### 2. Marking Attendance & Assigning Punishment

```
User clicks past meetup event
         │
         ▼
AttendanceModal opens
         │
User marks participants
         │
         ▼
markAttendance() action (for each)
         │
         └─→ Update meetup_participants.was_late
                   │
                   ▼
         PunishmentModal opens
                   │
User clicks "Assign Punishments"
                   │
                   ▼
         assignPunishment() action
                   │
                   ├─→ Select random punishment
                   │
                   └─→ Insert into event_punishments
                             │
                             ▼
                   Punishment displayed 🎲
```

### 3. Authentication Flow

```
User clicks "Sign in with Google"
         │
         ▼
Supabase Auth redirects to Google
         │
         ▼
User authorizes on Google
         │
         ▼
Google redirects to /auth/callback
         │
         ▼
Supabase creates/updates auth.users
         │
         ▼
handle_new_user() trigger fires
         │
         └─→ Create profile in profiles table
                   │
                   ▼
         User redirected to calendar
```

## Component Hierarchy

```
CalendarApp (Main orchestrator)
├── Sidebar
│   ├── StreakDisplay 🆕
│   ├── Member List
│   └── Action Buttons
├── CalendarHeader
│   └── View Toggles
├── CalendarGrid
│   ├── MonthView
│   ├── WeekView
│   └── DayView
├── Modals
│   ├── EventModal
│   ├── MeetupModal
│   ├── ShareModal
│   ├── AttendanceModal 🆕
│   └── PunishmentModal 🆕
└── AvailabilityView
```

## State Management

```
┌─────────────────────────────────────┐
│         React State (Local)         │
├─────────────────────────────────────┤
│ • currentDate                       │
│ • selectedDate                      │
│ • selectedEvent                     │
│ • view (month/week/day)            │
│ • showModals (boolean flags)       │
│ • visibleUsers                      │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│    Server State (Supabase)          │
├─────────────────────────────────────┤
│ • calendars                         │
│ • events                            │
│ • members                           │
│ • streaks 🆕                        │
│ • punishments 🆕                    │
└─────────────────────────────────────┘
```

## Security Layers

```
┌─────────────────────────────────────┐
│     1. Authentication Layer         │
│  Supabase Auth (Google/Facebook)    │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│   2. Authorization Layer (RLS)      │
│  • User can only see their cals     │
│  • Can only edit own events         │
│  • Members can see shared data      │
└─────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────┐
│    3. Application Layer             │
│  • Server-side validation           │
│  • Type checking (TypeScript)       │
│  • Input sanitization               │
└─────────────────────────────────────┘
```

## Feature Integration Points

### Streak System
```
Meetup Created → update_calendar_streak() → calendar_streaks updated
                                           → StreakDisplay refreshes
```

### Punctuality Tracking
```
Past Meetup Clicked → AttendanceModal → markAttendance()
                                      → PunishmentModal
                                      → assignPunishment()
                                      → event_punishments saved
```

## Technology Stack Layers

```
┌─────────────────────────────────────────────────┐
│              PRESENTATION LAYER                  │
│  React 19 + Tailwind CSS + Radix UI             │
└─────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────┐
│              APPLICATION LAYER                   │
│  Next.js 16 (App Router) + TypeScript           │
└─────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────┐
│                 API LAYER                        │
│  Server Actions + Supabase Client               │
└─────────────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────────────┐
│               DATABASE LAYER                     │
│  PostgreSQL (via Supabase)                      │
│  + Row Level Security                           │
│  + Database Functions                           │
└─────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Browser    │────▶│    Vercel    │────▶│   Supabase   │
│  (Frontend)  │     │  (Next.js)   │     │  (Backend)   │
└──────────────┘     └──────────────┘     └──────────────┘
      │                     │                      │
      │                     │                      │
  HTTPS/WSS            Server-side            PostgreSQL
  Connection            Rendering              Database
```

---

**Legend:**
- 🆕 = New in v2
- 🔄 = Modified in v2
- ✨ = Enhanced feature
- 🎲 = Randomized feature
